version: 2

models:
  - name: input__sales_orders_aerobase
    description: "Input model for sales orders"
    config:
      alias: sales_orders_aerobase
      tags: ['sales']
      group: sales
      access: private
  - name: input__sales_items_aerobase
    description: "Input model for sales items"
    config:
      alias: sales_items_aerobase
      tags: ['sales']
      group: sales
      access: private